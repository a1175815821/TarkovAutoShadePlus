# TarkovAutoShadePlus v2.0 终端用户视角评估报告

> 复核对象：TarkovAutoShadePlus v2.0.0（工作区代码，2026-09-19）
> 复核方式：对上一版报告的条目逐条在当前代码上重新检索取证，不再沿用旧结论
> 行号均取自本次复核时的实际文件；`src-wpf/MainWindow.xaml.cs` 简称 `MainWindow.cs`
>
> ⚠️ 本文是对 **v2.0.0** 的快照。v2.1 新增「色彩聚焦」后，`MainWindow.xaml.cs` /
> `MainWindow.xaml` 的行号已整体位移，引用这两处的行号需重新定位；
> 结论本身未变，未在 v2.1 中处理。

## 总体结论

v2.0 在**平滑度**和**双端调参**两个方向上做了实质改动，并修掉了长期存在的版本号不一致问题。这三点已经落地到代码里，可以从下面的取证结果直接验证。

但同时，上一版报告里被标为 P0 的几项——**Gamma 曲线被系统重置后不补回**、**显示器热插拔不刷新**——在 v2.0 中**没有任何改动**。这两项直接决定「玩着玩着滤镜没了」这类体验，仍是当前最值得优先处理的问题。

本次复核**没有做实机运行验证**。所有「已修复」指的是代码层面已按预期改动，实际观感（尤其是过渡平滑度）仍需在真实显示器上确认。

---

## 一、v2.0 已处理的问题

| # | 上一版问题 | 当前状态 | 证据 |
| --- | --- | --- | --- |
| R1 | 版本号三处不一致（顶栏 `v2.0` / 程序集 `2.0.0.0` / 关于窗口 `1.1.0`） | **已统一** | `MainWindow.xaml:50` 徽标 `v2.0`；`MainWindow.cs:2596` 关于窗口 `版本：2.0.0`；`Properties/AssemblyInfo.cs:21` `2.0.0.0`；`TarkovAutoShade.csproj:20` `<Version>2.0.0</Version>` |
| R2 | 过渡步数太少，短过渡只有 3~4 步，肉眼可见台阶 | **已改为时间驱动** | `GammaRampController.cs:14` 基准间隔由 75ms 降至 40ms；`:17` 新增 `MinimumTransitionSteps = 10`；`:205-215` 按 `时长/基准间隔` 算步数并夹到最少 10 步、单步不低于 16ms |
| R3 | 过渡进度按 tick 计数，受定时器抖动影响 | **已改用真实时钟** | `GammaRampController.cs:28` `Stopwatch transitionClock`；`:384-385` 用 `Elapsed / transitionDurationMilliseconds` 算进度；`:450-453` 停止时清理 |
| R4 | 没有免截图的实时跟随 | **已新增** | `src/RealtimeController.cs` + `src/RealtimeCapture.cs`；界面「实时全自动」面板 `MainWindow.xaml:226-257`（开关 / 采样间隔 600~3000ms / 三档灵敏度） |
| R5 | 实时抓屏的单帧抖动会让滤镜「呼吸」 | **已新增 EMA 自适应平滑** | `RealtimeController.cs:36-37` 慢/快时间常数 9.0s / 0.7s；`:290-336` `SmoothScene()` 对 P01~P99、EdgeEnergy、MeanRGB、Upper/LowerMean、BrightFraction 逐项按 `alpha = 1 - exp(-dt/tau)` 混合；`:307-312` tau 按误差幅度在快慢之间插值 |
| R6 | 夜视切换被平滑拖慢 | **已绕开平滑** | 夜视判定使用未经平滑的原始帧：`RealtimeController.cs` 中先以 raw 结果调用 `ImageAnalyzer.Complete`，再走 `IsNightVisionCross(lastAppliedRaw, raw)` |
| R7 | 原版与竞技场共用一套参数 | **已拆成两份档案** | `Models.cs:58-59` `EftProfile` / `ArenaProfile`；`:132-133` `ProfileKeyEft` / `ProfileKeyArena`；`:140-146` `ResolveGameKey()` 同时认进程名与截图路径；`MainWindow.cs:1288` `SwitchProfile()`；界面面板 `MainWindow.xaml:470-495` |
| R8 | 升级后老用户参数丢失 | **已做迁移** | `Models.cs:365-378` v12 迁移：把旧的一套参数复制成两份，竞技场额外 `-3` 亮度微调（`:137` `ArenaExposureTrim = 3`）；`:380-389` 对老 json 反序列化出的 null 做兜底 |
| R9 | 混合 DPI 下界面模糊/错位 | **manifest 已声明 PerMonitorV2**（仍有冗余调用，见 B8） | `app.manifest:27-28` `dpiAware=true/PM` 与 `dpiAwareness=PerMonitorV2, PerMonitor` |

---

## 二、复核后确认仍未解决的问题

### P0 —— 直接影响「能不能用」

| # | 问题 | 证据 | 影响 | 建议 |
| --- | --- | --- | --- | --- |
| A1 | **Gamma 曲线被系统/驱动重置后不自动补回** | `Reapply()` 只有定义（`GammaRampController.cs:220`），在 `src/`、`src-wpf/` 全仓库检索无调用点；`DisplaySettingsChanged`、`WM_DISPLAYCHANGE`、`PowerModeChanged`、`SessionSwitch`、`SystemEvents` 全部**无匹配** | 分辨率变化、驱动刷新、锁屏唤醒、远程桌面断开后曲线被系统还原，表现为「玩着玩着滤镜没了」，且程序毫无察觉 | 订阅显示设置变更 / 电源 / 会话事件，并加 3~5 秒轻量校验：读回 ramp 与期望值比对，不一致即 `Reapply()` |
| A2 | **显示器热插拔 / 分辨率变化不自动刷新列表** | `RefreshDisplayList` 仅两个调用点：`MainWindow.cs:123`（初始化）与 `:889`（「刷新显示器」按钮） | 拔屏后曲线可能仍挂在已失效的设备；新接入的显示器完全不会被纳入 | 复用 A1 的事件订阅触发刷新；拔屏时先恢复该屏曲线再通知用户 |

### P1 —— 影响「好不好用」

| # | 问题 | 证据 | 影响 | 建议 |
| --- | --- | --- | --- | --- |
| A3 | **截图只认 PNG** | `ScreenshotWatcher.cs:113` `new FileSystemWatcher(folder, "*.png")`；`:144` `GetFiles(folder, "*.png")`；`:183` 再次校验扩展名为 `.png` | 用第三方工具输出 jpg/bmp 的用户完全无法触发 | 扩展常见格式，或在目录不可用时明确提示支持的格式 |
| A4 | **一次截图分析内整图解两次，拖滑块再解一次** | `MainWindow.cs:1999` `ImageAnalyzer.Analyze(filePath, snapshot)` 内部解码一次，紧接着 `:2000` `LoadStableBitmap(filePath)` 再解码一次；`RefreshCurrentPreview()` 在 `:2117` 又解一次 | 4K 截图多次全解码，明显浪费 | 一次解码后复用，或让 `Analyze` 直接返回已解码位图 |
| A5 | **动任意滑块会静默把预设切成「自定义预设」** | `MainWindow.cs:853` `PresetComboBox.SelectedIndex = 5` | 用户不知道自己改了什么、原预设去哪了 | 显示「已修改（基于：室内柔和）」徽标，并提供「另存为新预设 / 放弃修改回到原预设」 |
| A6 | **预览区空状态文案是硬编码，与真实状态无关** | `MainWindow.xaml:383` `<TextBlock Text="状态：监听中" ...>` 是静态文本 | 目录未配置或监听已停止时仍显示「监听中」，误导 | 绑定到 `screenshotWatcher.IsActive`，区分「监听中 / 未监听 / 目录不可用」 |
| A7 | **数值滑杆的整数吸附不统一** | 全仓库只有 `MainWindow.xaml:245`（实时间隔）与 `:539`（暗部可见度）开了 `IsSnapToTickEnabled` | 其余滑杆拖动时会产生小数，显示却按整数取整，读数与实际值不一致 | 全部数值滑杆统一开启整数吸附 |
| A8 | **实时面板文案作出实现不成立的承诺** | `MainWindow.xaml:256` 写「切出游戏自动暂停」；但前台判断 `IsGameForeground`（`MainWindow.cs:1545`）与「滤镜自动启停」开关绑定 | 「滤镜自动启停」关闭时，切到浏览器/桌面实时抓屏照旧运行 | 文案改为条件描述，或让实时模式自带独立的前台检测 |
| A9 | **滑块变更后无条件唤醒实时抓屏** | `UpdateCurrentRecommendation()` 末尾 `MainWindow.cs:2164` `realtimeController.Wake()` | 拖动滑块过程中每次防抖结束都会打断实时循环并额外抓屏一次 | 只在滑杆松手（`PreviewMouseUp` / `LostMouseCapture`）时唤醒，或合并为一次 |
| A10 | **OBS 联动完全没有界面** | `ObsFilterStateStore` 在 6 处写文件：`MainWindow.cs:102`、`:1396`、`:2231`、`:2251`、`:2276`、`:2961`；目标文件 `obs-filter-state.taslut`（`ObsFilterStateStore.cs:16`）；界面无开关与说明，本仓库也不含 OBS 插件 | 用户不知道程序一直在往本地写这个文件 | 设置里加「OBS 联动」开关、文件路径与当前状态，并写入文档 |
| A11 | **无障碍属性缺失** | `MainWindow.xaml`、`Styles/TacticalStyles.xaml`、`App.xaml` 中 `AutomationProperties` 与 `FocusVisualStyle` **均无匹配** | 键盘焦点与屏幕阅读器支持薄弱 | 补 `AutomationProperties.Name` 与可见焦点样式 |

### P2 —— 细节与一致性

| # | 问题 | 证据 | 影响 | 建议 |
| --- | --- | --- | --- | --- |
| B8 | **DPI 感知存在重复声明** | `app.manifest:27-28` 已声明 PerMonitorV2，但 `App.xaml.cs:29` 仍额外调用 `SetProcessDPIAware()` | 冗余。通常 manifest 优先，但两套机制并存容易在后续维护中产生误解 | 移除 `SetProcessDPIAware()` 调用，只保留 manifest |
| B9 | **手动关闭滤镜后，一张截图就会把它重新打开** | `filterModeEnabled` 初值为 `true`（`MainWindow.cs:47`），且在 `:2092`、`:2183`、`:2191`、`:2232`、`:2257` 多处被无条件置回 `true` | 游戏内截图是高频动作（记录战利品、报点），「我只是想先关掉」的意图被反复覆盖 | 增加「手动关闭后保持关闭，直到手动开启」选项 |
| B10 | **切换参数档案时的连锁反应靠隐式标志位挡住** | `SwitchProfile()`（`MainWindow.cs:1288`）会调用 `ApplySettingsToSliders()`，进而触发滑块 `ValueChanged` 与 `PresetComboBox.SelectionChanged`；目前靠 `switchingProfile` 标志位在处理器首行拦截 | 保护是隐式的，后续新增处理器很容易漏掉，一旦漏掉就会出现「切档案把另一端的参数冲掉」 | 把判断集中到一处，或改用显式的抑制作用域 |

---

## 三、本次未复核的条目

上一版报告还列了以下方面的问题，本次复核**没有逐条重新取证**，因此不给出结论，仅列出供后续处理：

- 预设体系：无法命名、保存多套、导入导出
- 没有「锁定当前曲线 / 纯手动模式」
- 无开机自启与启动行为设置；关闭行为一旦「记住选择」后无界面入口改回
- 无日志面板与诊断导出；通知是单条覆盖式、3 秒后清空
- 无「恢复原始曲线」的独立常驻入口
- 无首屏引导/自检向导
- 仅中文；主窗口最小尺寸 1060×700 在低分辨率 + 高缩放下可能超出可视区
- 无分析历史（只保留最近一次）
- 快捷键对话框不支持 Win 键、无禁用项、无冲突预检
- 预览分割线命中区仅 4px；不支持拖入图片、放大、全屏对比
- DDC/CI 写入失败不回滚 UI；每次切换显示器都重新探测
- 实时状态文字缺少解释；「实时状态」与「前台状态」两个读数难以对应
- 大图分析期间无进度、无取消，超时原因不具体
- 设置 450ms 防抖写盘较频繁；通知计时器未复用

---

## 四、建议的处理顺序

1. **A1 + A2**（P0）：补事件订阅 + 周期性 ramp 校验。这两项决定了滤镜在长时间游玩中的可靠性，且实现上是同一套机制。
2. **A3 + A4**：格式支持与解码复用，改动小、收益直接。
3. **A5 + A6 + A7**：界面诚实性问题，会让用户误解当前状态。
4. **A8 + A9**：实时模式的文案与性能。
5. **A10 + A11**：可见性与无障碍。
6. **B8 + B9 + B10**：一致性清理。

> 再次提醒：R2 / R3 / R5 / R6 的效果需要在真实显示器 + 真实游戏画面上确认，本报告只验证了代码改动存在。
